const fs = require('fs'); const path = require('path'); const p = path.join(__dirname, 'firebase-service-account.json'); if (fs.existsSync(p)) { const raw = fs.readFileSync(p, 'utf8'); const b64 = Buffer.from(raw).toString('base64'); console.log('\nCopy this base64 string to Render environment variable FIREBASE_SERVICE_ACCOUNT_BASE64:\n'); console.log(b64); console.log('\n'); } else { console.log('firebase-service-account.json not found in backend folder!'); }
